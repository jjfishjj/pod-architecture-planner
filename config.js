window.PROJECT={short:'POD ARCHITECTURE PLANNER',kicker:'PROJECT 03 · INFRASTRUCTURE',name:'BasePOD／SuperPOD 架構規劃器',lead:'把模型規模、資料量、使用者與可用性需求轉換成 GPU 節點、網路、儲存與機房容量。',tags:['DGX','BasePOD','SuperPOD','NVAIE'],labTitle:'AI 叢集容量估算器',labCopy:'以工作負載需求估算 GPU、節點、網路與儲存吞吐，並判斷較合適的架構級別。',controls:[{id:'model',label:'模型參數量',min:7,max:1000,step:1,value:70,format:n=>n+'B'},{id:'users',label:'同時訓練工作',min:1,max:32,step:1,value:4,format:n=>n+' jobs'},{id:'dataset',label:'活躍資料集',min:1,max:1000,step:1,value:100,format:n=>n+' TB'},{id:'gpuMem',label:'單 GPU 記憶體',type:'select',options:[[80,'80 GB'],[141,'141 GB'],[180,'180 GB']]}],calculate:v=>{let modelBytes=v.model*2.4,perJob=Math.ceil(modelBytes/(v.gpuMem*.65)),gpus=Math.max(8,Math.ceil(perJob*v.users/8)*8),nodes=Math.ceil(gpus/8),storage=Math.max(20,gpus*.8),score=Math.min(100,gpus/256*100);let tier=gpus<=16?'DGX／Certified System':gpus<=128?'DGX BasePOD':'DGX SuperPOD';return{main:gpus,unit:'GPU',score,sub:[[nodes,'8-GPU 節點'],[storage.toFixed(0)+' GB/s','建議讀取吞吐'],[tier,'建議級別']],verdict:`${tier} 較符合目前規模；再依 checkpoint、網路 collective 與 SLA 校正。`}},steps:[['定義工作負載','拆分訓練、推論、開發與多租戶。'],['估算記憶體','計入權重、optimizer、activation 與餘裕。'],['設計三張網','計算、儲存、管理 fabric 分離規劃。'],['評估營運','納入排程、NVAIE、監控與支援。'],['機房驗證','確認電力、散熱、重量與擴展空間。']],metrics:[['≥ 80%','目標 GPU 使用率'],['≥ 85%','多節點 scaling'],['N+1','關鍵服務可用性'],['< 5 min','checkpoint 目標']],risks:[['只數 GPU','忽略網路、儲存與人員並行需求。'],['混淆 NVAIE','NVAIE 是企業軟體平台，不是硬體拓撲。'],['低估供電','高密度 AI 機櫃需要完整機電評估。'],['單一 Fabric','管理流量與訓練流量互相干擾。']]};
